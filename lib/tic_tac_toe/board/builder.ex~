defmodule TicTacToe.Board.Initial_State do
  require Utils
  
  @dir Utils.module_path

  def get(size) do
    __MODULE__
    |> safe_concat("BoardSize" <> Integer.to_string(size))
    |> apply(:get, [])
  end


  def build do

  end



end
